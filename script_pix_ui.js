const fs = require('fs');
let c = fs.readFileSync('src/components/cart/CartDrawer.tsx', 'utf8');
c = c.replace(`</div>\n                  })\n\n                  {/* Resumo do pedido */}`, `</div>\n                  )}\n\n                  {paymentMethod === 'pix' && pixKey && (\n                    <div className=\"bg-green-50 p-3 rounded-lg border border-green-200 space-y-2\">\n                      <label className=\"text-green-800 text-xs font-bold flex flex-col gap-0.5\">\n                        <span>Chave PIX para Pagamento</span>\n                      </label>\n                      <div className=\"flex items-center gap-2\">\n                        <Input readOnly value={pixKey} className=\"bg-white border-green-300 h-9 text-sm font-bold\" />\n                        <Button \n                          type=\"button\" \n                          size=\"icon\" \n                          variant=\"outline\" \n                          className=\"h-9 w-9 shrink-0\" \n                          onClick={() => {\n                            navigator.clipboard.writeText(pixKey);\n                            toast({ title: \"Chave PIX copiada!\", description: \"Cole no seu aplicativo do banco.\" });\n                          }}\n                        >\n                          <Copy className=\"h-4 w-4\" />\n                        </Button>\n                      </div>\n                      {\n                        pixName && <p className=\"text-xs text-muted-foreground\">Titular: <strong>{pixName}</strong></p>\n                      }\n                      <p className=\"text-[10px] leading-tight text-green-800/80\">\n                        Faça o pagamento e <strong>envie o comprovante no nosso WhatsApp</strong> para liberação do pedido!\n                      </p>\n                    </div>\n                  )}\n\n                  {/* Resumo do pedido */}`);
fs.writeFileSync('src/components/cart/CartDrawer.tsx', c);
