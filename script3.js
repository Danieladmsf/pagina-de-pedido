const fs = require('fs');
let c = fs.readFileSync('src/components/cart/CartDrawer.tsx', 'utf8');
c = c.replace('})\n                      </div>\n                    )}', `})\n                      </div>\n                    )}\n\n                    {paymentMethod === \\\'conta_casa\\\' && baseDeliveryFee > 0 && (\n                      <div className="bg-purple-50 p-2 rounded-lg border border-purple-200 space-y-1.5">\n                        <Label className="text-purple-800 text-xs font-bold flex items-center gap-2 cursor-pointer">\n                          <input\n                            type="checkbox"\n                            checked={payDeliverySeparately}\n                            onChange={(e) => setPayDeliverySeparately(e.target.checked)}\n                            className="rounded border-purple-300 text-purple-600 focus;ring-purple-500 h-4 w-4" />\n                          <span className="leading-tight">Remover taxa do Prazo (Pagar R$ {baseDeliveryFee.toFixed(2)} ao entregador)</span>\n                        </Label>\n                      </div>\n                    )}`);
fs.writeFileSync('src/components/cart/CartDrawer.tsx', c);
